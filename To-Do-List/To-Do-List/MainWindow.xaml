<Window x:Class="To_Do_List.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="To-Do List" Height="500" Width="800" ResizeMode="NoResize">

    <Grid Margin="10">
        <DataGrid x:Name="TaskGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserResizeColumns="False"
                  ColumnWidth="*"
                  Margin="0,0,0,70">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                <DataGridTextColumn Header="Task Name" Binding="{Binding task}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding description_task}" Width="330"/>
                <DataGridTextColumn Header="Due Date" Binding="{Binding date_task, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridCheckBoxColumn Header="Status" Binding="{Binding COMPLETED}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Height="60" Margin="0,10,0,0">
            <Button x:Name="Add_Button" Content="Add" Width="100" Height="50" Margin="5" Click="Add_Click" FontSize="20" FontWeight="Bold"/>
            <Button x:Name="Read_Button" Content="Read" Width="100" Height="50" Margin="5" Click="Read_Click" FontSize="20" FontWeight="Bold"/>
            <Button x:Name="Update_Button" Content="Update" Width="100" Height="50" Margin="5" Click="Update_Click" FontSize="20" FontWeight="Bold"/>
            <Button x:Name="Remove_Button" Content="Remove" Width="100" Height="50" Margin="5" Click="Remove_Click" FontSize="20" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>