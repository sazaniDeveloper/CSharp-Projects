<Window x:Class="To_Do_List.ReadTasks"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:To_Do_List"
        mc:Ignorable="d"
        Title="ReadTasks" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="TaskGrid"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          CanUserAddRows="False"
          CanUserResizeColumns="False"
          ColumnWidth="*"
          Margin="0,0,0,70">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="50"/>
                <DataGridTextColumn Header="Task Name" Binding="{Binding task}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding description_task}" Width="330"/>
                <DataGridTextColumn Header="Due Date" Binding="{Binding date_task, StringFormat=d}" Width="120"/>
                <DataGridCheckBoxColumn Header="Status" Binding="{Binding COMPLETED}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
